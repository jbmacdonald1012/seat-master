export async function create(req, res) {
  res.status(501).send('Not implemented');
}

// Named 'remove' to avoid shadowing the built-in delete keyword
export async function remove(req, res) {
  res.status(501).send('Not implemented');
}
