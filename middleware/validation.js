// Validation middleware stubs — will be implemented in future sessions
// using express-validator or manual field checks

export const validateLogin = (req, res, next) => next();

export const validateRegister = (req, res, next) => next();

export const validateContact = (req, res, next) => next();
